// ====================================================================
// School Connect Demonstration College — School Connect Configuration
// Generated by School Connect Wizard (HMG Concepts)
// ====================================================================
// This is the LIVE DEMO config for REPLACE-WITH-YOUR-DEMO-DOMAIN.example.com. It connects to a
// shared demo Supabase project so the public preview is fully interactive.
//
// NOTE on contract alignment (FIX G3/G6): this file now matches the structure
// that Generator.generateConfigJS() emits for every fresh client ZIP — same
// window.SCHOOL field set (including theme/layout/font), same window.SC
// globals (esc/jsStr/slugify, THEMES, MODULES), same Supabase bootstrap, and
// the same free-channel notification flags. A brand-new client download ships
// with YOUR_SUPABASE_URL / YOUR_SUPABASE_ANON_KEY placeholders (see README);
// this demo simply has them pre-filled so the public site works out of the box.
// ====================================================================
window.SC = window.SC || {};
window.SCHOOL = {
  name: 'School Connect Demonstration College',
  shortName: 'SCD',
  motto: 'A fully simulated school — explore every feature',
  theme: { id: 'theme15', primary: '#0506ae', accent: '#964eec' },
  layout: 'layout0',
  font: { id: 'plusjakarta', family: 'Plus Jakarta Sans', css: 'Plus+Jakarta+Sans' },
  address: 'HMG Demo Campus, Lagos, Nigeria',
  phone: '2348100866322',
  email: 'buildingmyictcareer@gmail.com',
  siteUrl: 'https://REPLACE-WITH-YOUR-DEMO-DOMAIN.example.com',
  campuses: [],
  hmgLink: 'https://hmgconcepts.pages.dev/',
  logoExt: 'svg',
  primary: '#0506ae',
  accent: '#964eec',
  currency: '₦',
  admissionAcronym: 'SCD',
  // v13 ENT-BILL: site license — this showcase deployment is a one-time
  // (lifetime) purchase: no recurring fees, the school owns it forever.
  license: { model: 'lifetime', plan: 'One-time purchase (lifetime ownership)', cycle: '', started_on: '', expires_on: null, grace_days: 7, status: 'active', renew_url: '', lock_message: '' },
  licenseRegistryUrl: '',
  licenseSalt: '',
  // v13 ENT-DEMO: prospective-client showcase. Guests explore a fully simulated
  // school via the one-tap sign-in panel on login.html (accounts created by
  // database/demo-users.sql; data by database/demo-seed.sql — see DEMO-SETUP.md).
  demo: { enabled: true, note: 'Interactive demo — sample data only, resets periodically. Do not enter real student data.' }
};

// Catalog mirrors (populated by the builder at generation time; [] in the
// shipped site — runtime reads SCHOOL.* directly, never the catalog).
window.SC.THEMES = [];
window.SC.MODULES = [];

// Shared escaping/serialisation helpers (defence-in-depth for any inline render).
window.SC.esc = function (s) {
  return String(s == null ? '' : s).replace(/&/g, '&amp;').replace(/</g, '&lt;').replace(/>/g, '&gt;').replace(/"/g, '&quot;').replace(/'/g, '&#39;');
};
window.SC.jsStr = function (s) { return JSON.stringify(String(s == null ? '' : s)); };
window.SC.slugify = function (s) { return String(s || '').toLowerCase().replace(/[^a-z0-9]+/g, '-').replace(/^-|-$/g, ''); };

// Supabase client initialisation — paste YOUR demo project URL + anon key above.
const SUPABASE_URL = 'https://qbjoyytchrgfhacjauts.supabase.co';
const SUPABASE_ANON_KEY = 'eyJhbGciOiJIUzI1NiIsInR5cCI6IkpXVCJ9.eyJpc3MiOiJzdXBhYmFzZSIsInJlZiI6InFiam95eXRjaHJnZmhhY2phdXRzIiwicm9sZSI6ImFub24iLCJpYXQiOjE3ODQ3OTI3NDMsImV4cCI6MjEwMDM2ODc0M30.Kvf34SHb05JP7MRhXcvF1PathTSCvHQA-8Gst5OFwio';

// Always expose safe globals so public/offline pages never throw ReferenceError.
window.sb = null;
var sb = null;

if (SUPABASE_URL && SUPABASE_URL !== 'YOUR_SUPABASE_URL' && !SUPABASE_URL.includes('YOUR_') &&
    window.supabase && SUPABASE_ANON_KEY && !SUPABASE_ANON_KEY.includes('YOUR_')) {
  window.sb = window.supabase.createClient(SUPABASE_URL, SUPABASE_ANON_KEY, {
    auth: { persistSession: true, autoRefreshToken: true }
  });
  sb = window.sb;
  console.log('[School Connect] Connected to Supabase at', SUPABASE_URL);
} else {
  console.warn('[School Connect] Supabase not configured. Edit assets/js/config.js with your URL + anon key (see README.md).');
}

// ============================================================
// FREE NOTIFICATION CHANNELS — enabled by default (no API keys)
// ============================================================
window.SC_CONFIRM_FREE_EMAIL = true;
window.SC_CONFIRM_FREE_WA = true;
window.SC_CONFIRM_FREE_SMS = true;

console.log('[School Connect] Config loaded — School Connect Demonstration College — Powered by HMG Concepts');
